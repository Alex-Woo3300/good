public class PushbackPlanner {

    // TODO: figure out real parameter types/order from BoggleGameTest.java calls
    // and from the compiler errors that come back once this stub is in place.
    public static Choice[] findWinningSequence(Object... args) {
        return new Choice[0];
    }
}
